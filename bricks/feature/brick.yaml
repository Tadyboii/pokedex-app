name: feature
description: Generates a complete feature scaffold following Clean Architecture

version: 1.0.0

environment:
  mason: ">=0.1.0 <1.0.0"

vars:
  name:
    type: string
    description: Feature name in PascalCase (e.g., Auth, Dashboard, Transaction)
    prompt: What is the feature name? (PascalCase)
